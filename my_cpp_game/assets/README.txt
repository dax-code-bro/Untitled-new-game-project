Textures, models and sounds live here.
Nothing is checked in yet: the JS engine generates every texture
procedurally at run time (46 recipes in engine/src/40-material.js) and
builds every mesh from code, so there are no source assets to copy.
That is a porting decision to make, not a file to move.
